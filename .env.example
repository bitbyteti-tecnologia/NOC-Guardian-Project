# Variáveis de ambiente para NOC - Guardian (exemplo)
# Copie este arquivo para `.env` e preencha com valores reais no servidor.
#
# ATENÇÃO: Nunca commite o arquivo `.env` com segredos. Já está ignorado no .gitignore.
#
# E-mail para registro do certificado SSL (Let's Encrypt)
ACME_EMAIL=admin@seu-dominio.com

# Token usado pela API Central para validar requisições de ingestão
CENTRAL_TOKEN=SEU_TOKEN_FORTE

# Token usado pelo NODE para enviar Authorization: Bearer
AUTH_TOKEN=SEU_TOKEN_FORTE

# Tamanho máximo de payload aceito pela Central (bytes). Padrão: 1048576 (1MB)
TELEMETRY_MAX_BYTES=1048576

# Intervalo de coleta do NODE (segundos). Padrão: 30
NODE_INTERVAL_SECONDS=30
